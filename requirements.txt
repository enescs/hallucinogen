# Python 3.10 or newer: fastapi, uvicorn and mcp all require it, and the version
# floor of this project is theirs rather than anything the code itself needs.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
# Only for mcp_server.py, which is how Claude drives the browser and, with
# HLG_LLM=claude, becomes the model writing its pages. The browser itself runs
# without it.
mcp>=2.0
